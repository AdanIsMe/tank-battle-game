#include "player.h"
#include <iostream>


// Constructor implementation
Player::Player(){}

int Player::nextMove()
{
    //run algorithm an decide action
    //for now we have only one tank so we run this action on the tank we have
    
    //return strategy(gameboard);
    return 0;
}
